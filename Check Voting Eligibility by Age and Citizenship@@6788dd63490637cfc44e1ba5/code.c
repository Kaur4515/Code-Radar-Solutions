#include <stdio.h>
int main() {
    int a , b;
    if ((a>=18 && b==1)){
        printf("Eligible");
    }
    if else((a<=18) && (b==0)){
        printf("Not Elgible");
    }
    return 0;
}